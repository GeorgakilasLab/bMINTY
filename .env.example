# bMINTY Docker Configuration
# Copy this file to .env and customize as needed

# Port Configuration
# Change these if the default ports are already in use on your system
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Frontend API Configuration (automatically derived from BACKEND_PORT)
# The React app runs in the browser and automatically connects to:
# http://localhost:{BACKEND_PORT}/api
# Only override this if you need a different host or API path:
# REACT_APP_API_BASE=http://localhost:8000/api
